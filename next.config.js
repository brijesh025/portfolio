/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.jsdelivr.net",
      "img.icons8.com",
      "assets.vercel.com",
      "www.vectorlogo.zone",
    ],
  },
};

module.exports = nextConfig;
